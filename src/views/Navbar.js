import React from "react";

const Navbar = () => {
  return (
    <div>
      <header>
        <h1>Proyects App</h1>
        <button>members</button>
        <button>new team</button>
      </header>
    </div>
  );
};

export default Navbar;
